Capturas de tela do dashboard Node-RED (Fase 3, Parte 2):

- `dashboard-node-red.png` — interface em `/ui` (gráfico, gauge, estado sem alerta ou conforme a captura).
- `alerta-temperatura.png` — mesma interface com condição de **alerta** ativa.

Grafana não integra esta entrega (item opcional do enunciado).
